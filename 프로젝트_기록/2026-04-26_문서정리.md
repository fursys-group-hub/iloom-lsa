# 2026-04-26 문서 정리 + 탭 통합

## 한 일

### 1. 기수 요약 탭 통합
- 기존 독립 페이지(`/dashboard/batch-summary`)를 AI 분석 리포트 페이지의 탭으로 통합
- `ReportsPageClient.tsx` 래퍼 생성 — "AI 리포트" / "기수 요약" 언더라인 탭
- ref 패턴으로 자식 컴포넌트 액션(새로고침, PDF 다운로드)을 부모 헤더 버튼에 연결
- 사이드바에서 "기수 요약" 메뉴 제거, 독립 페이지 삭제

### 2. 스타일 수정
- h1 → h2 (fontSize: 28 고정) 수정 — DESIGN_SYSTEM.md 기준 준수
- 제목-탭 간격: marginBottom → 부모 gap: 24로 통일 (questions 페이지 패턴 참고)
- `--shadow-lg` CSS 변수 누락 버그 수정 (라이트+다크 모두)

### 3. DESIGN_SYSTEM.md 업데이트
- §4: h2 fontSize를 clamp() → 28 고정으로 수정
- §3: `--shadow-lg` 정의 추가 (sm=카드, md=모달, lg=토스트)
- §7-A 신설: 페이지 레이아웃 패턴 (flex column gap 24, 액션 버튼 배치, marginBottom 금지)
- 날짜 2026-04-26으로 업데이트

### 4. MOBILE_UI.md 업데이트
- §4 전면 재작성: 5행 → 4개 서브섹션(A~D), 30개 이상 반응형 그리드 패턴 문서화
  - 4-A: 관리자 대시보드 (25개 항목)
  - 4-B: 교육생 마이페이지 (5개 항목)
  - 4-C: auto-fill/auto-fit 자동 반응형 (8개 항목)
  - 4-D: 반응형 미적용 남은 과제 (2개)
- §7: 5개 과제 중 3개 완료 체크 (테스트분석, 교육효과, 마이페이지)

### 5. 통합 기획서 작성
- `/통합_기획서.md` — 개인 기록 + 회사 보고 겸용
- 비개발자 언어로 28개 이상 페이지 기능 설명
- 교육 효과 측정 전략, 향후 계획 포함

## 삽질/이슈
- h2 fontSize를 처음에 clamp로 잡아서 크기 틀림 → DESIGN_SYSTEM.md 문서 자체가 outdated였던 게 원인
- 제목-탭 간격도 gap이 아닌 marginBottom으로 처리해서 좁았음 → 기존 페이지(questions) 패턴 확인 후 수정
- dev 서버 응답 없는 문제 → 포트 킬 후 재시작으로 해결

## 다음에 할 것
- 심화교육 종합(overview) 점수 스케일 모바일 반응형 처리
- 매장관리자 총평(final) 점수 스케일 모바일 반응형 처리
