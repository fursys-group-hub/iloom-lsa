# 2026-04-07 — 리포트 실습일지 반영 + Supabase DB 정리

## ✅ 오늘 한 것:

### 1. 홈 대시보드 차시별 평균 개선
- 정렬: `localeCompare` → 숫자 추출 정렬 (10차시가 1차시 앞에 오던 버그 수정)
- 색상 4단계: 80+ 초록 / 70+ 파랑 / 60+ 주황 / 60 미만 빨강

### 2. AI 코칭 리포트 — 실습일지 반영
- 실습일지 step3(칭찬)/step4(보완)을 **관찰자 시점 전문적 요약**으로 변환
- 원문 복붙 → 키워드 분석 기반 생활기록부 톤 (`synthesizePractice()`)
- 강점: 자기 효능감, 단독 응대, 프레젠테이션 역량 등 자동 감지
- 보완: 심리적 불안감, 소재 설명, 실측, 프로모션 안내 등 자동 감지
- 추천 교육 스타일 + 첫 주 교육에도 실습일지 키워드 반영

### 3. 리포트 레이아웃 개선
- 왼쪽 고정: 📋 피드백 + 💡 추천 교육 + 🎯 첫 주 교육 (하나의 흐름)
- 오른쪽 고정: 📊 체크리스트 + 📈 성적 + 🚨 반복 오답
- 매장 실습 관찰 영역 디자인: 구분선 + 소제목 + 실적 미니카드 + 불릿 리스트
- 다크모드/라이트모드/인쇄 CSS 대응 (CSS 변수 활용)

### 4. generate-reports.js 스크립트 통합
- 기존 v1(메모 리라이트 + 상세 분석) + v2(실습일지) 통합
- `/tmp` 임시 파일 의존 제거 → DB 직접 수집
- `.env.local` 자동 로드 (환경변수 하드코딩 제거)
- Supabase 1000건 리밋 회피 (학생별 개별 쿼리)
- `node scripts/generate-reports.js [batchId]` 한 줄로 실행

### 5. Supabase DB 정리
- P6 테이블 8개 삭제: exams, exam_sessions, exam_groups, exam_members, product_references, question_product_links, question_history, import_logs
- 레거시 테이블 2개 삭제: wrong_answers, teaching_insights
- questions 컬럼 9개 삭제: sub_question_id, question_main, image_path, is_active, product_flag 등
- test_responses.exam_id 컬럼 삭제
- P6 인덱스 5개 삭제
- schema.sql 현재 DB 상태에 맞게 갱신 (questions, test_responses, students, batches 컬럼 반영)

### 6. 빌드 에러 수정
- `JSX.Element` → `React.ReactElement` (React 19 호환)
- `subjectCategory` useState 유니온 타입 명시

### 🤔 결정한 것:
- 실습일지 원문을 리포트에 직접 노출하지 않음 → 키워드 분석 후 관찰자 시점으로 변환
- 리포트 레이아웃: 피드백+추천교육+첫주 = 왼쪽 고정 (하나의 흐름이므로 분리 불가)
- wrong_answers 테이블 삭제 (test_responses.is_correct로 대체됨)
- P6 역량평가 관련 테이블/컬럼 전부 삭제 (독립 프로젝트로 분리됨)

### ⏭️ 다음에 할 것:
- Phase 5: 자동 동기화 cron, 기수 아카이브
- 독립 시험 플랫폼 상세 기획 + 구현
- 교육 자료 관리 (Phase 6)
