# 2026-04-23 — 매장관리자 심화테스트 & 사이드바 정비

## 한 일

### 1. R&P 평가에서 AI 리포트 탭 제거
- `/manager/evaluations/page.tsx`에서 `viewTab` 상태, useEffect, UI 탭 버튼 전부 제거
- 이제 R&P 평가 페이지는 주차별 평가 내용만 표시

### 2. 심화테스트 페이지 신규 생성
- `/manager/tests/page.tsx` 새로 만듦
- 교육생별 탭 선택 → `/api/advanced-student-overview?student_id=` 호출
- 주차×차시 12개 카드 그리드 (1주차 1차시 ~ 6주차 2차시)
- 카드: 점수 / 반 평균 / 오답 수 / 달성·미달성 뱃지
- 카드 클릭 → `SessionDetailModal` 팝업 (학생 이름 포함)
- 기존 `/my/advanced-tests` 구조를 그대로 가져오되 교육생 탭 선택 레이어 추가

### 3. 홈 화면 전체 교육생 표시
- `/manager/page.tsx`에서 `storeName` 상태 제거
- `activeStudents` 필터에서 `storeName` 조건 제거 → 전체 교육생 표시 (퇴사자·기수 필터만 유지)

### 4. 사이드바 메뉴명 정리
- `/manager/layout.tsx` nav 배열 수정
  - `인수인계 리포트` → `입문교육 AI 리포트`
  - `테스트` → `심화테스트` (준비중 disabled 도 제거)
  - `R&P` → `주차별 R&P 평가`
  - `교육 총평` → `심화교육 총평`

## 특이사항
- 없음, 순탄하게 진행됨

## 다음에 할 것
- 미정
