# 2026-04-27 — 교육내용정리 시스템 구축 (1차 인프라 + 코펜하겐 기준 모델)

## ✅ 오늘 한 것

### 1. 5개 데이터 소스 자동 수집 인프라 구축

| 소스 | 스크립트 | 결과 |
|------|---------|------|
| 학생 일지 (Supabase `student_notes`) | `fetch-series-data.mjs` | 시리즈 키워드 기반 추출 |
| 일룸 PPT 자료 (`기존자료/*.pptx`) | `lib/pptx-parser.ts` (adm-zip + xml2js) | 통합 PPT 1~584장만 일룸 |
| Notion 단종/사양변경 DB | `fetch-notion-discontinued.mjs` | 2-1) 프로젝트 NOTION_TOKEN 활용 |
| WordPress 제품 가이드 (5탭) | `fetch-product-guide.mjs` (Playwright) | 5탭 + 표 + 이미지 자동 추출 |
| 세트마스터 ERP 엑셀 | `read-set-master.mjs` | 4,107행 분석 |

### 2. 일룸 카탈로그 자동 수집

- `fetch-catalog.mjs 158 160 154 52 150` 실행 → `public/iloom-catalog.json` 222개 시리즈
- 카테고리 5개 (리빙룸 60, 다이닝룸 27, 침실·옷장 42, 키즈룸·틴즈룸 40, 워크룸·멀티룸 6)
- is_target 175개 (단종·구버전 제외)

### 3. 일지 분류 시스템 (정규식 매칭)

- 시리즈명 94개 기반 정규식 매칭
- Gemini 안 쓰고 정규식으로 5초만에 처리 (Gemini였다면 5분 소요)
- `textbook_classifications` 테이블 1,836건 누적

### 4. 코펜하겐 챕터 = 기준 모델 완성

- `scripts/textbook/output/series-코펜하겐/chapter.html` 350줄, 22KB
- DB `textbook_chapters` 테이블에 저장
- 4소스 통합 (PPT + 일지 23건 + Notion + WordPress 5탭)
- 색상 칩 12종, 사이즈 도면 포함
- 이미지 31개 (16MB) → `public/textbook-images/코펜하겐/`

#### 챕터 섹션 순서 (반드시 이 순서로)

1. 📋 기본 정보 + 🚨 매장 영업 필수 안내(빨간 박스)
2. ⭐ 제품 특장점 (한 문장 정의 → 영업 멘트 → 페르소나)
3. 💡 매장 실전 팁 (일지 인용)
4. 🎓 학생 깨달음 사례 (blockquote)
5. ─── (구분선) 사양 정보 ───
6. 🪑 운영 라인업 (마감재별 표)
7. 🎨 마감재 / 색상 옵션 (가로 배치)
8. 🔧 소재 / 내장재
9. 📐 사이즈 도면

### 5. 시스템 인프라

- **DB 스키마**: `textbook_sources` / `textbook_classifications` / `textbook_chapters`
- **API 라우트 6개**: catalog / classify / draft / notes-pool / route(CRUD) / source-pptx
- **UI 페이지 3개**:
  - `/dashboard/textbook` 메인 (카드 그리드, 카드 숨김)
  - `/dashboard/textbook/[series]` 검수/편집
  - `/dashboard/textbook/print` 16:9 가로 PDF 인쇄

### 6. 사이드바 메뉴

- "교육내용정리"로 등록 (성과분석 그룹, 교육설문 다음, 교육인사이트 위)
- ❌ "통합 교재" 같은 명칭 사용 안 함

## 🤔 결정한 것 (39개 사항, 기획서 참고)

핵심만 발췌 — 전체는 [`docs/교육내용정리-기획서.md`](../docs/교육내용정리-기획서.md) "사용자 결정 사항" 참고.

### 데이터 정책
- 교육일지 + 자율학습 사용 / **실습일지 제외** (해석 패턴 다름)
- 모든 기수 누적
- 온라인 전용 제품은 별도 표기

### AI 정책
- **Gemini 사용 안 함** — 응답 부정확, 시간 오래 걸림
- **Claude(Code)가 직접 일지 분석**해서 영업 노하우 작성
- 사양 정보(코드/사이즈/색상)는 **자동화 스크립트**가 담당

### 분류 방식
- AI 분류 ❌ → **정규식 매칭** ✅ (5분 → 5초)
- 시리즈명 직접 추출 (`series-map.ts` 정적 매핑은 fallback)

### 카탈로그
- "(구)" 접두사 → 자동 단종 / "(뉴)" → 신버전 인지
- 2025/2026 NEW 라벨 표시 안 함
- 온라인 라벨만 시리즈명 옆 inline pill로 표시
- 카드 우상단 ✕로 부속품 카드 숨김 (브라우저 localStorage 저장)
- 숫자/날짜만 있는 시리즈명 자동 제외 (26.04, 2023, 1, P 등)

## ⏭️ 다음에 할 것 (우선순위 순)

1. **자동 챕터 생성 통합 스크립트** (`generate-chapter.mjs <시리즈명>`)
2. **우선순위 시리즈 챕터 일괄 생성** (로이 98건 → 뉴트 70건 → 링키플러스 47건 → ...)
3. **세트마스터를 Supabase 테이블로 업로드** (현재 엑셀로만)
4. **침실·옷장 카테고리 표 구조 정밀 파싱** (현재 pumok/gubun 빈 문자열)

> ⚠️ 이날 작업 후 git push 직전에 멈춤 — 다음 날(2026-04-28)에 push + 누락 파일 보강 + 일괄 데이터 인프라 구축으로 이어짐.
