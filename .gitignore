node_modules
.next
.env.local
.env

# 노션 raw export (CSV/HTML 덤프) - 파싱해서 lessons-data.ts로 생성
프로젝트_기록/ExportBlock-*/
ExportBlock-*/
프로젝트_기록/*.xlsx

# 개인정보 보관 (학생 사진, 이력서 등 — 절대 커밋 금지)
_private/

# 안전망 — 혹시 모를 민감 파일 실수 방지
app/*.jpg
app/*.jpeg
app/*.png
app/*.pdf
**/이력서*.pdf
**/합격자*.pdf
