# 2026-05-27 — 콘텐츠 동기화 + 시뮬레이터 12종 확장 + Netlify 배포 준비

## ✅ 오늘 한 것:

### 1. 인쇄본 (`키큰옷장_한샘비교_v2-6(부품에서 타사 없음).html`) 콘텐츠 수정

**4페이지 — 부품 인트로 문장 톤다운**
- 변경 전: "가구 가격의 30% 이상은 보이지 않는 부품(레일·힌지·금속)에 들어갑니다."
- 변경 후: "가구의 품질은 눈에 잘 보이지 않는 부품(레일·힌지·금속)에서 차이가 납니다."
- 비율 수치(30%)를 빼고 "품질 차이"를 직접 짚는 표현으로 교체

**5페이지 — 모듈에 별표 표시 + 각주 교체**
- `서랍 옷장*` — 작은 sup 별표 추가
- `하부선반 하프장**`, `멀티선반 하프장**` — 이중 별표 추가
- 기존 도어 일괄 안내 각주를 두 줄로 분리:
  - `*` 해당 이미지는 리디 도어 서랍앞판 적용 + 도어(리디/스톤W) 선택 시 외관 차이 + 옷걸이봉 탈부착 가능
  - `**` 이동형 선반으로 편의에 맞게 공간 구획 가능

**7페이지 — 직접비교 회피**
- "인피니 화이트 도어 450폭/500폭" → "동급 화이트 도어 450폭/500폭"
- 한샘 도어 제품명 노출을 피해 익명화

---

### 2. 웹 앱 (`쇼룸_웹/index-v2.html`) 동기화

**4페이지 문장 동기화** — 인쇄본과 동일하게 교체

**서랍 옷장 이미지 인쇄본과 통일**
- 인쇄본은 `연경님 전달본/F1774337547949NHKZLILEPMX_m.jpg`, 웹은 다른 사진을 쓰고 있던 차이 발견
- 인쇄본 이미지를 웹 폴더의 `3단 서랍 옷장.jpg` 자리에 복사 (코드 수정 없이 파일만 교체)

**모듈 시뮬레이터 6종 → 12종 확장**
- 기존: 각 모듈에 폭 1종(900폭 또는 450폭)만 추가 가능
- 변경: 각 모듈의 900/1000폭(또는 450/500폭)을 별도 카드로 분리
  - 기본장 900폭 / 1000폭
  - 긴장 900폭 / 1000폭
  - 속서랍 옷장 900폭 / 1000폭
  - 서랍 옷장 900폭 / 1000폭
  - 하부선반 하프장 450폭 / 500폭
  - 멀티선반 하프장 450폭 / 500폭
- 카드 제목에 파란색 폭 표기 강조 (예: `기본장 1000폭`)
- 같은 base 모듈은 같은 이미지 공유
- 시각 바: `Math.round(m.width / 900 * 140) + 'px'` 비례 계산 (900→140, 1000→156, 450→70, 500→78)
- 추가 시 총 폭 자동 정확 계산 (900 vs 1000 구분)
- 타이틀: "모듈 6종" → "모듈 12종 (900/1000폭 · 450/500폭)"

---

### 3. Netlify 배포 준비

**참조 자산 전수조사**
- HTML에서 실제 참조하는 이미지: 16개
- HTML에서 참조하는 폰트: 4개 (Paybooc Light/Medium/Bold/ExtraBold)
- 외부 CDN 의존성: 0개 (완전 오프라인 가능)

**미사용 파일 9개 삭제**
- `salice.jpg`, `hettich.jpg` (로고지만 코드에서 안 씀)
- `LPM PET.png`
- `멀티선반 하프장.png` (jpg 버전만 사용)
- `F1571028…`, `F1543384…`, `F1542363…`, `F1630378…`, `image_7693584…` (로그성 미사용 이미지)
- 삭제 후 `사용 이미지본/` 폴더는 정확히 16개 파일만 남음

**`_redirects` 파일 유지** — `/` → `/index-v2.html` 자동 리다이렉트

**Netlify 비밀번호 보호 안내**
- 배포 후 "Please enter your password" 메시지 발생 시 → Netlify 사이트 설정(Access & security → Password protection)에서 Disabled로 변경

---

## 🤔 결정한 것:

- **30%라는 수치를 빼기로** — 출처가 명확하지 않은 비율보다 "품질 차이"를 직접 표현하는 게 신뢰감 있음
- **타사 제품명(인피니)은 익명화** — 직접 비교를 피하는 원칙 유지
- **시뮬레이터는 카드 분리 방식** — 한 카드 안에 버튼 2개 두는 대신 12개 카드로 만들어서 시각적으로 명확
- **이미지는 같은 파일 공유** — 900/1000폭, 450/500폭은 실제 외관 차이가 거의 없어 같은 이미지로 충분
- **인쇄본 ↔ 웹앱 콘텐츠 동기화 원칙 유지** — 한쪽만 바꾸면 안 되고 두 파일 모두 수정

---

## ⏭️ 다음에 할 것:

- Netlify 배포 후 QR 코드로 매장 비치 옵션 검토
- 시뮬레이터 12종 확장 후 사용성 점검 (스크롤 부담 여부)
- 시뮬레이터 카드 정렬 순서/그룹핑 개선 여지 (모듈별 두 폭이 나란히 보이게)
