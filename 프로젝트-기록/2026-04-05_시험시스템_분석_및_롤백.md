# 2026-04-05 — 시험 시스템 분석 + 구현 + 롤백

## ✅ 오늘 한 것

### 1. GAS CBT 시스템 전체 분석 (04-04)
- `LSA_Final.gs` (267줄) + `LSA_Final_Deploy_최종.html` (2186줄) 분석
- 7가지 채점모드 파악: OX, 객관식_단일, 객관식_복수, 주관식_단답, 허용답, 순서무관, 순위차등, 서술
- cleanAndNorm 정규화 로직: 한글 공백 제거, 선행 숫자 추출, toUpperCase
- 빈칸 `()` 파싱, 소문항 그룹핑, maxSelect 제한 등 핵심 로직 파악

### 2. 갭 분석 문서 작성 (04-04)
- GAS vs Next.js 10개 갭 발견: CRITICAL 3개 / HIGH 3개 / MEDIUM 2개 / LOW 2개
- 4-Phase 로드맵 수립 (채점엔진 → DB스키마 → CBT UI → 통합)

### 3. Phase 1~3 구현 (04-04~05)
- **Phase 1**: `lib/grading.ts` — cleanAndNorm() 함수, 순위차등 부분점수, toUpperCase 통일
- **Phase 2**: DB 스키마 확장 — questions에 parent_num, sub_id, main_text 추가 + /api/sync 수정
- **Phase 3**: CBT UI 보강 — 빈칸 인라인 input, 소문항 main_text 헤더, maxSelect 제한, /exam 독립 진입 페이지

### 4. 전체 테스트 통과 (04-05)
- 채점 로직: 한글 공백 정규화, 순위차등 부분점수, 순서무관 등 전부 검증
- API: verify-code, take (answer_count 전달, correct_answer 제거) 정상
- 빌드 성공 (`npm run build`)

### 5. 방향 전환 — 전부 롤백 (04-05)
- **결정**: 시험 생성/관리를 입문교육 앱 안에 넣지 않고, 독립 시험 플랫폼으로 분리
- 이유: 입문교육생뿐 아니라 내부직원/LSA도 시험을 봐야 함. 구글 시트 의존도 제거 필요
- `git checkout . && git clean -fd` → 마지막 push 상태(`4a31e3e`)로 완전 복원
- Supabase에서 exams, exam_questions, exam_submissions 테이블 + Phase 2 컬럼 삭제 예정

### 6. 새 시험 플랫폼 기획 시작
- 기존 입문교육 사이트 안에서 확장하되, `/test` 독립 라우트 + `/dashboard/questions-bank` 관리 페이지
- 웹 UI 직접 문제 입력 + 엑셀 업로드 둘 다 지원
- 응시자 사전 등록 방식 (접근코드 + 이름/소속)
- 구글 시트 동기화 레거시 유지 (기존 `/dashboard/tests`)

## 🤔 결정한 것
- 시험 시스템은 입문교육 앱에 종속시키지 않음 → 독립 플랫폼으로 확장
- 구글 시트 의존 제거 — 웹에서 직접 문제 입력/수정 → Supabase 즉시 반영
- 기존 입문교육 기능(질문관리, 교육일지, 출결 등)은 전혀 영향 없음 확인 완료
- grading.ts 채점 로직은 새 프로젝트에서 재사용 예정

## ⏭️ 다음에 할 것
- Supabase 정리 (exams 관련 테이블 삭제 SQL 실행)
- 독립 시험 플랫폼 상세 기획 + 구현 시작
  - Step 1: DB 확장 + 문제은행 API
  - Step 2: 문제은행 관리 UI (`/dashboard/questions-bank`)
  - Step 3: 응시자 관리 + `/test` 독립 응시 페이지
