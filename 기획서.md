# 일룸 신입사원(LSA) 교육 관리 대시보드 — 기획서

> **작성일**: 2026-03-26
> **작성자**: 수지 + Claude Code
> **상태**: Phase 1 착수 전

---

## 1. 프로젝트 배경

### 현재 프로세스 (AS-IS)
1. 구글 폼으로 매일 시험 진행
2. 수기로 결과 분석 (엑셀/노션)
3. 관리자에게 구두 또는 문서로 보고
4. 학생별 피드백은 개별 메시지로 전달

### 문제점
- 시험 결과 분석에 매일 1~2시간 소요
- 학생별 취약점 추적이 어려움 (누적 관리 부재)
- 관리자 보고 형식이 매번 다름
- 학생이 자기 성적/취약점을 실시간으로 볼 수 없음

### 목표 (TO-BE)
- Google Sheets 데이터 자동 수집 → DB 저장
- 차트 기반 시각화 대시보드
- AI 기반 코칭 리포트 자동 생성
- 학생 개인 페이지에서 성적/오답/코칭 확인

---

## 2. 사용자 및 역할

| 역할 | 사용자 | 접근 범위 |
|------|--------|----------|
| **교육자** (educator) | 수지 | 전체 대시보드 + 모든 학생 관리 + 설정 |
| **관리자** (manager) | 팀장/본부장 | 리포트 열람 + 전체 현황 조회 (편집 불가) |
| **학생** (student) | 신입사원 10~20명 | 자기 페이지만 (/my) |

---

## 3. 기술 스택

| 항목 | 선택 | 근거 |
|------|------|------|
| 프레임워크 | Next.js 16 App Router + TypeScript | iloom-banner와 동일, 최신 버전 |
| 스타일 | Tailwind CSS v4 + Pretendard | 일관성 유지, 한국어 최적 폰트 |
| 상태관리 | Zustand 5 (persist) | iloom-banner 패턴 재사용 |
| DB | Supabase (PostgreSQL) | 무료 티어, Auth 통합, RLS |
| 차트 | Recharts | React 네이티브, 커스텀 용이 |
| AI 코칭 | Claude Code (대화형) | API 비용 $0, 세밀한 분석 |
| 시험 데이터 | Google Sheets API v4 | 구글 폼 응답 읽기 전용 |
| 배포 | Vercel | 환경변수 관리, 자동 배포 |

---

## 4. 디자인 시스템

### 컨셉
**Linear + Apple iOS** — 다크모드 기본, 깔끔한 여백, 부드러운 라운딩, 미니멀 컬러

### 타이포그래피
- **폰트**: `Pretendard` (CDN: `cdn.jsdelivr.net/gh/orioncactus/pretendard`)
- **기본 크기**: 16px, line-height 1.6
- **최소 크기**: 12px 이하 절대 사용 금지
- **위계**: font-weight로 구분
  - Regular(400): 본문
  - Medium(500): 라벨, 네비게이션
  - Semibold(600): 소제목, 태그
  - Bold(700): 제목, 강조 수치
  - ExtraBold(800): 대시보드 큰 숫자

### 색상 시스템 (다크모드)
```css
/* 배경 — 4단계 깊이 */
--bg-main:      #0A0A0A    /* 최상위 배경 */
--bg-surface:   #141414    /* 카드, 사이드바 */
--bg-elevated:  #1C1C1E    /* 입력 필드, 모달 */
--bg-hover:     #242424    /* hover 상태 */

/* 테두리 */
--border:       #2C2C2E    /* 기본 구분선 */
--border-light: #3A3A3C    /* 보조 구분선 */

/* 텍스트 — 4단계 투명도 */
--text-primary:  #FFFFFF           /* 제목, 이름 */
--text-second:   rgba(235,235,245, 0.8)  /* 본문 */
--text-tertiary: rgba(235,235,245, 0.6)  /* 비활성 메뉴 */
--text-muted:    rgba(235,235,245, 0.3)  /* 보조 텍스트 */

/* 의미 색상 */
--blue:    #007AFF   /* 주요 액션, 선택된 메뉴 */
--blue-light: #0A84FF
--blue-dim: rgba(10,132,255, 0.15)  /* 파란색 배경 */
--green:   #30D158   /* 양호, 정답 */
--orange:  #FF9F0A   /* 주의, 부분 정답 */
--red:     #FF453A   /* 위험, 오답 */
--purple:  #BF5AF2   /* 채점모드 태그 */
```

### 반지름 토큰
| 토큰 | 값 | 용도 |
|------|-----|------|
| `--radius-sm` | 8px | 작은 버튼, 입력 필드 내부 |
| `--radius-md` | 12px | 카드, 입력 필드, 버튼 |
| `--radius-lg` | 16px | 큰 카드, 테이블 컨테이너 |
| `--radius-xl` | 20px | 로그인 카드 |
| `--radius-pill` | 9999px | 뱃지, 태그 |

### 레이아웃 패턴
- **사이드바**: 240px (관리자) / 220px (학생), 고정 position
- **콘텐츠 영역**: `maxWidth: 1400px` (관리자) / `1200px` (학생), 가운데 정렬
- **모바일**: 768px 이하에서 사이드바 숨김 + 햄버거 메뉴
- **카드 패턴**: `background: var(--bg-surface), border: 1px solid var(--border), borderRadius: var(--radius-lg), padding: 24px`

### 주요 컴포넌트 패턴

**카드**
```css
background: var(--bg-surface);
border: 1px solid var(--border);
border-radius: var(--radius-lg);
padding: 24px;
```

**버튼 (Primary)**
```css
background: var(--blue);
color: #fff;
border: none;
border-radius: var(--radius-md);
padding: 12px 24px;
font-weight: 600;
```

**버튼 (Ghost)**
```css
background: transparent;
color: var(--text-tertiary);
border: 1px solid var(--border);
border-radius: var(--radius-md);
padding: 10px 20px;
```

**뱃지/태그**
```css
border-radius: var(--radius-pill);
padding: 4px 12px;
font-size: 13px;
font-weight: 600;
/* 색상별: green-dim+green, red-dim+red, orange-dim+orange, blue-dim+blue */
```

**상태 뱃지**
- 🟢 양호 (green): 80점 이상 + 출결 양호
- 🟡 주의 (orange): 60-80점 또는 지각 3회+
- 🔴 위험 (red): 60점 미만 또는 결석 2회+

**접이식 (details/summary)**
- 카테고리별 학습 현황, 차시별 오답, AI 인사이트에 사용
- hover 시 `background: var(--bg-hover)` 전환

**차트 (Recharts)**
- 다크모드: 격자 `#2C2C2E`, 축 라벨 `#8E8E93`
- 라인: 파란색(학생/본인) + 회색(반 평균)
- 툴팁: `background: #1C1C1E, border: #3A3A3C`

**정답률 바**
- 높이 6px, 둥근 모서리
- 80%+ 초록, 60-80% 주황, 60% 미만 빨강

---

## 5. 데이터 소스 — Google Sheets

### 시트 정보
- **시트 ID**: `1_2pc1Mr05DeZMPdaILpPKtOhAsD53XtIWHHJpLqBRtI`
- **시트명**: `LSA 입문교육 문제 (1)`

### 문제_db 탭 (gid=553160028) — ~800행, 14차시

| 컬럼 | 내용 | 예시 |
|------|------|------|
| 차수 | 교육 차시 | `1차시` ~ `14차시` |
| 번호 | 차시별 문제번호 | `1`, `2`, `15` |
| 대분류 | 가구 카테고리 | `브랜드`, `학생방`, `침실`, `주방` |
| 시리즈 | 제품 시리즈명 | `공통`, `팅클팝`, `로이` |
| 상세 | 학습 주제 | `기업가치/브랜드`, `제품 규격`, `색상 옵션` |
| 질문_메인 | 문제 텍스트 | 빈칸, 선택, OX 등 |
| 보기 | 객관식 선택지 | `슬로우베드\|데스커\|시디즈` (파이프 구분) |
| 정답 | 정답 | `LPM\|엘피엠` (파이프 구분) |
| 채점모드 | 문제 유형 | `주관식_순서무관`, `객관식_단일`, `OX` |
| 배점 | 점수 | `1` ~ `8` |
| 해설 | 해설 텍스트 | 200자 이내 |
| 이미지 | 이미지 URL | `https://i.ibb.co/...` (선택) |

### 결과_db 탭 — 학생별 시험 결과

| 컬럼 | 내용 | 예시 |
|------|------|------|
| A: 일시 | 제출 시간 | `2026-03-24 8:41:59` |
| B: 차수 | 차시 | `1차시` |
| C: 이름 | 학생 이름 | `곽현서`, `김강욱` |
| D: 점수 | 획득 점수 | `78` |
| E: 총점 | 만점 기준 | `82` |
| F: 100점 만점 | 환산 점수 | `95.12` |
| G: 오답노트 | 틀린 문제 상세 | 아래 구조 참고 |
| H: 노션전송 | 전송 상태 | `완료` |

### 오답노트(G열) 파싱 구조

```
Q9. 일룸에서 가장 많이 사용하는 마감재는 ()이다.
- 제출: 라미네이트
- 정답: LPM|엘피엠
- 해설: 일룸 가구 표면재로 가장 널리 쓰이는 것은 LPM입니다.

Q10. LPM은 PB, MDF 등의 구조재에 접착제와...
- 제출: O
- 정답: X
- 해설: 틀린 설명입니다.
```

**파싱 로직**:
1. `Q숫자.` 로 문제 분리
2. `- 제출:`, `- 정답:`, `- 해설:` 로 각 필드 추출
3. 문제번호를 문제_db와 조인 → 대분류/시리즈/상세 태그 자동 매핑

---

## 6. 데이터베이스 스키마 (Supabase)

```sql
-- 교육 기수
CREATE TABLE batches (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,                -- '2026년 3월 신입 1기'
  start_date DATE NOT NULL,
  end_date DATE NOT NULL,
  sheet_id TEXT,                     -- Google Sheets ID
  subject_columns JSONB NOT NULL     -- {"색상": {"column":"C","maxScore":10}, ...}
);

-- 교육생
CREATE TABLE students (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  batch_id UUID REFERENCES batches(id),
  name TEXT NOT NULL,
  department TEXT,
  UNIQUE(batch_id, name)
);

-- 시험 점수 (Sheets에서 동기화)
CREATE TABLE test_scores (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  student_id UUID REFERENCES students(id),
  test_date DATE NOT NULL,
  subject TEXT NOT NULL,
  score NUMERIC(5,2) NOT NULL,
  max_score NUMERIC(5,2) DEFAULT 100,
  UNIQUE(student_id, test_date, subject)
);

-- 출결
CREATE TABLE attendance (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  student_id UUID REFERENCES students(id),
  date DATE NOT NULL,
  status TEXT CHECK (status IN ('present','late','early_leave','absent')),
  note TEXT,
  UNIQUE(student_id, date)
);

-- 교육자 메모
CREATE TABLE student_memos (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  student_id UUID REFERENCES students(id),
  date DATE NOT NULL,
  content TEXT NOT NULL,
  category TEXT DEFAULT 'general'
);

-- 오답 기록
CREATE TABLE wrong_answers (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  student_id UUID REFERENCES students(id),
  test_date DATE NOT NULL,
  subject TEXT NOT NULL,
  question_summary TEXT,
  tags TEXT[] NOT NULL,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- AI 코칭 리포트
CREATE TABLE coaching_reports (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  student_id UUID REFERENCES students(id),
  test_date DATE NOT NULL,
  student_message TEXT NOT NULL,
  manager_report TEXT NOT NULL,
  tag_tracking JSONB,
  created_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(student_id, test_date)
);

-- 학생 교육 노트
CREATE TABLE student_notes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  student_id UUID REFERENCES students(id),
  title TEXT NOT NULL,
  content TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- 공지사항 (관리자 → 기수별)
CREATE TABLE announcements (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  batch_id UUID REFERENCES batches(id) ON DELETE CASCADE,
  title TEXT NOT NULL,
  content TEXT NOT NULL,
  priority TEXT CHECK (priority IN ('normal', 'important', 'urgent')) DEFAULT 'normal',
  created_at TIMESTAMPTZ DEFAULT now()
);

-- 교육일지 코멘트 (관리자↔교육생 대화)
CREATE TABLE note_comments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  note_id UUID NOT NULL REFERENCES student_notes(id) ON DELETE CASCADE,
  author_role TEXT NOT NULL CHECK (author_role IN ('admin', 'student')),
  author_name TEXT NOT NULL,
  content TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

---

## 7. 데이터 흐름

```
[Google Forms] → [Google Sheets]
                    ├── 문제_db 탭 (문제+정답+해설+태그)
                    └── 결과_db 탭 (학생별 점수+오답노트)
                          │
                    Sheets API v4
                          │
                    [/api/sync] ── 오답노트 파싱 ──→ wrong_answers 테이블
                          │                              (태그 자동 매핑)
                    [Supabase DB]
                          │
          ┌───────────────┤
          ▼               ▼
   [Dashboard]     [/api/reports]
   [Recharts]      PDF/HTML 리포트
```

---

## 8. AI 코칭 시스템

### 핵심 원칙
Claude API를 쓰지 않는다. **Claude Code(나)가 Supabase DB를 직접 읽고 코칭을 생성하여 DB에 저장**한다.

### 워크플로우
```
[웹앱] 수지가 "오늘 결과 분석" 버튼 클릭
   ▼ 분석 요청 프롬프트 생성 + 클립보드 복사
[Claude Code] 수지가 프롬프트 붙여넣기
   ▼ Supabase DB에서 데이터 읽기
   ▼ 20년차 베테랑 강사 페르소나로 코칭 생성
   ▼ coaching_reports 테이블에 저장
[웹앱] 최신 코칭 자동 표시
```

### 분석 버튼 4종

| 버튼 | 트리거 | 결과 |
|------|--------|------|
| 오늘 결과 분석 | 매일 시험 후 | 학생별 선생님 메시지 + 관리자 코칭 |
| 분야별 분석 | 과목 교육 완료 시 | 과목별 학생 분석 + 출제 개선 추천 |
| 주간 분석 | 매주 금요일 | 주간 변화 + 태그 추적 |
| 종합 분석 | 교육 종료 시 | 전체 성장 곡선 + 최종 진단 |

### 페르소나

**학생용 (iMessage 선생님 톤)**:
> "민수야, 오늘 색상 파트에서 많이 올랐네! 소재 쪽은 내일 목재 등급표 한번 더 보자~"

**관리자용 (전문 코칭 리포트)**:
- 영역별 성장 추적 (극복 / 신규 약점 / 고질적 약점)
- 총평 및 진단
- 오답 기반 코칭
- 종합 코칭 전략
- 격려의 한마디

### 취약 태그 추적

```typescript
interface TagTracking {
  overcome: string[];    // 과거 취약 → 이번에 극복
  newWeak: string[];     // 이번에 새로 등장한 취약점
  chronic: string[];     // 계속 반복되는 고질적 약점
}
```

---

## 9. 핵심 화면 구성

### 메인 대시보드 (`/dashboard`)
- 오늘 출석 현황 카드 (출석/지각/결석)
- 반 전체 평균 점수 추이 LineChart (최근 2주)
- 위험 학생 알림 카드 (평균 60점 미만 or 결석 2회+)
- 오늘 과목별 평균 BarChart

### 학생 상세 (`/dashboard/students/[id]`) — 핵심 페이지
- 학생 프로필 카드
- 점수 추이 LineChart (일별 총점 + 과목별)
- 과목 강약점 RadarChart
- 태그 추적 카드 (극복/신규/고질적 뱃지)
- 출결 캘린더 히트맵
- 교육자 메모 타임라인
- AI 관리자 리포트 (접이식 카드)

### 학생 개인 페이지 (`/my`) — 학생이 직접 사용
- 선생님 메시지 (iMessage 스타일 말풍선)
- 내 성적 차트 (LineChart + RadarChart)
- 오늘의 학습 포인트
- 교육 노트 (마크다운 에디터)
- 오답 모아보기 (태그별 정리)

### 관리자 뷰
- 반 평균 추이 + 개인별 산점도
- "지켜봐야 할 학생" 리스트
- AI 코칭 리포트 열람

---

## 10. 위험 학생 판별 로직

```
high:   최근 3일 평균 < 60 OR 결석 2회+
medium: 특정 과목 3회 연속 < 70 OR 지각 3회+
low:    전 과목 >= 70, 출결 양호
```

---

## 11. 페이지 라우팅 구조

```
app/
├── page.tsx                       # → /login 리디렉트
├── login/page.tsx                 # 로그인
│
├── dashboard/                     # 교육자/관리자 전용
│   ├── layout.tsx                 # 사이드바 + 헤더
│   ├── page.tsx                   # 오늘의 요약
│   ├── students/
│   │   ├── page.tsx               # 학생 목록 + 필터
│   │   └── [id]/page.tsx          # 개별 학생 상세
│   ├── attendance/page.tsx        # 출결 관리
│   ├── reports/
│   │   ├── weekly/page.tsx        # 주간 리포트
│   │   └── subject/page.tsx       # 과목별 리포트
│   └── settings/page.tsx          # Sheets 연동, 기수 관리
│
├── my/                            # 학생 개인 페이지
│   ├── layout.tsx                 # 학생용 레이아웃
│   ├── page.tsx                   # 홈 (메시지 + 성적)
│   ├── scores/page.tsx            # 성적 추이
│   ├── notes/
│   │   ├── page.tsx               # 노트 목록
│   │   └── [id]/page.tsx          # 노트 편집
│   └── wrong-answers/page.tsx     # 오답 모아보기
│
└── api/
    ├── sync/route.ts              # Sheets → Supabase
    ├── coaching/route.ts          # 코칭 저장
    ├── students/route.ts
    ├── attendance/route.ts
    ├── memos/route.ts
    ├── notes/route.ts
    └── reports/route.ts
```

---

## 12. 구현 순서

### Phase 1 — MVP: 데이터 + 차트 ✅ 완료
1. ✅ Next.js 프로젝트 생성 + Supabase 연결
2. ✅ DB 스키마 (batches, students, test_scores, questions, test_responses)
3. ✅ Settings 페이지 (기수/학생 관리)
4. ✅ `/api/sync` — Sheets 3탭(문제은행+결과_DB+상세_로그) → Supabase 동기화
5. ✅ 메인 대시보드 (반 평균 차트, 학생 목록)
6. ✅ 학생 상세 (점수 추이, 태그별 학습 피드백, 카테고리별 현황, 차시별 오답)
7. ✅ 시험 관리 (차시별 성적, 문제은행 정답 수정+재채점, 서술형 수동 채점)
8. ✅ Excel 다운로드 (결과_DB + 상세_로그 + 차시별)

### Phase 1.5 — 출결 + 로그인 ✅ 완료
1. ✅ 출결 관리 (Excel 임포트 + 수정)
2. ✅ 로그인 페이지 + 관리자 인증
3. ✅ 사이드바 레이아웃
4. ✅ Railway 배포

### Phase 2 — 학생 개인 페이지 + 노트 ✅ 완료
1. ✅ `/my` 학생 개인 페이지 (홈/출결/테스트/교육일지)
2. ✅ 내 성적 + 오답 모아보기 (반 평균 비교, 해설 포함)
3. ✅ 학습 피드백 태그 (약점/주의/강점)
4. ✅ 교육일지 작성 (자신감 체크 + 태그 + 핵심 필기)
5. ✅ 로그인 분기 (관리자→/dashboard, 학생→/my)
6. ✅ 학생별 비밀번호 (기수 관리에서 설정)

### Phase 2.5 — AI 교육 인사이트 ✅ 완료
1. ✅ Gemini 2.5 Flash 기반 오답 분석 자동 생성
2. ✅ 홈 화면 AI 인사이트 카드
3. ✅ 취약 영역 + 교육 추천 + 변형 문제 제안

### Phase 3 — 커뮤니케이션 + 관리 도구 ✅ 완료
1. ✅ 📢 공지사항 (기수별 관리자→교육생, 로그인 팝업 + 모아보기)
2. ✅ 💬 교육일지 코멘트 (관리자↔교육생 대화형, iMessage 스타일)
3. ✅ 기수관리에서 퇴사 처리 + 비밀번호 초기화
4. ✅ 퇴사자 교육일지 회색 처리 + 로그인 차단
5. ✅ 📱 모바일 반응형 전면 개선 (패딩/버튼/날짜/카드 간결화)
6. ✅ 사이드바 영업지원 챗봇 링크

### Phase 3.5 — 홈 화면 + 교육자 도구 (다음)
1. 홈 화면 개선 (교육자 관점 최적화)
2. 교육자 메모 (MemoEditor + MemoTimeline)
3. 학생 비밀번호 변경 (학생이 /my에서 직접)
4. 질문하기 (학생 → 교육자)

### Phase 4 — 교육/실습 + AI 코칭
1. 교육 페이지 (교육 자료 관리)
2. 실습 페이지 (실습 기록)
3. AI 코칭: 학생별 선생님 메시지 (Claude Code 직접)
4. AI 코칭: 관리자 리포트 (분석 버튼 4종)

### Phase 5 — 편의기능 (선택)
1. 자동 동기화 cron
2. 카카오톡/이메일 발송
3. 기수 아카이브
4. Google Sheets 역동기화 (대시보드 수정 → 시트 반영)

---

## 13. 검증 방법

1. 테스트 Google Sheet + 더미 데이터 10명 x 5과목 x 5일
2. Settings에서 Sheet 연결 → 동기화 → Supabase 확인
3. 대시보드 차트 렌더링 확인
4. 학생 상세 점수 추이/레이더 차트 확인
5. 출결 기록 → 위험 학생 플래깅 동작 확인
6. Vercel 배포 후 사내 접속 테스트
